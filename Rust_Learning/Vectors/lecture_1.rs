fn main() {
    // let pizza_diameters: Vec<i32> = Vec::new();
    // let pizza_diameters = Vec::<i32>::new();
    let pizza_diameters = vec![5, 7, 34, 23, 4];
    println!("{:?}", pizza_diameters);

    let pastas: Vec<&str> = Vec::new();
    println!("{:?}", pastas);
}
